server:
  port: 8082

spring:
  application:
    name: content-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:vyroflix}?currentSchema=content}
    username: ${SPRING_DATASOURCE_USERNAME:${POSTGRES_USER:vyroflix}}
    password: ${SPRING_DATASOURCE_PASSWORD:${POSTGRES_PASSWORD:vyroflix}}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        default_schema: content
        format_sql: true

  flyway:
    enabled: ${FLYWAY_ENABLED:true}
    schemas: content
    default-schema: content

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics
  endpoint:
    health:
      show-details: when_authorized
      probes:
        enabled: true
